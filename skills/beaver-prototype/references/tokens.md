# Beaver design tokens

> **Auto-generated** by `pnpm beaver:sync`. Reflects whatever the live
> `@<inner-ds>/design-tokens` package exports.

This file is the only legal source of style values for prototypes. Any
`style={{}}` or color/size override must reference one of these.

## Imports

```ts
import { colors, spacing, typography, animation } from '@<inner-ds>/design-tokens';
```

(Sub-paths like `@<inner-ds>/design-tokens/colors` are also fine — the
runtime exposes both forms.)

## Catalog

After `beaver:sync` runs, this section will be populated with the actual
groups, paths, and example values from your installed `@<inner-ds>/design-tokens`
package. Until then, refer to the package's own `*.d.ts` files (e.g.
`packages/design-tokens/src/colors.d.ts`, `animation.d.ts`).

### colors

_Pending sync._

### spacing

_Pending sync._

### typography

_Pending sync._

### animation

_Pending sync._
